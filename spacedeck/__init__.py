"""spacedeck — spaced review over your course notes.

Cards are Markdown files with frontmatter. The engine advances them along a
two-rung ladder (recall, derive) and regenerates a queue view. Nothing here
writes card bodies: the encoding is the user's to author.
"""

__version__ = "0.1.2"
